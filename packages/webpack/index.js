import React from 'react';
import ReactDOM from 'react-dom/client';

const FirstComponent = () => {
  return <h1>Hi Momsdf</h1>;
};

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<FirstComponent />);
